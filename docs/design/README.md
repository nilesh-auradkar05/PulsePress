# HTML Design Visuals

These HTML files are portfolio-ready visual companions. They are not the best source for coding agents because HTML/SVG adds layout noise. Pair each file with the matching Markdown companion in `docs/design-md/`.
